# 正则短词匹配 - 逻辑梳理

## 原因

匹配是「用户输入 ⊇ 关键词」。只有「正则语法」时，输入「正则」不包含该长词 → 零命中。

## 修复

`regex.cheat` 增加短入口词，并给 `正则` 高 `keyword_weights`，使光说「正则」进速查（tips 再引导「5个汉字」等具体说法）。

## 分工

| 输入 | 期望 |
|------|------|
| 正则 / 写个正则 | `regex.cheat` |
| 5个汉字 / 汉字正则 | `regex.len.han` |
| 5个数字 | `regex.len.digit` |
