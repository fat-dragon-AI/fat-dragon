# 构建目录权限与架构隔离 — 逻辑梳理

```text
qemu-arm64 docker（root）
  → 写 build/pyi、dist/lch（属主 root）
本机用户 build_release
  → rm 失败「权限不够」

修复：
  chown 归还属主
  workpath → build/pyi-${ARCH_TAG}（x64 / arm64 分开）
  清理失败 → 提示 sudo chown
```
