# 逻辑梳理：多命中选意图

```text
query
  → hits
  → len==1: format_hit_detail
  → len>=2: format_hit_list → 选> n → format_hit_detail
       查询模式: browse 可多次选
       Agent: select 一次后进入 agent> 执行；i 回列表
```

编号两层含义分开：

| 提示符 | 编号含义 |
|--------|----------|
| `选>` | 意图（匹配列表） |
| `agent>` | 当前意图下的命令（runnables） |
