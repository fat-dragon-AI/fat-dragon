# 变更记录：硬件与系统配置类规则

| 项 | 内容 |
|----|------|
| 日期 | 2026-08-10 |
| 文件 | `resources/rules.json` |

## 新增意图（6 条）

| intent_id | 用途 | 典型命令 |
|-----------|------|----------|
| `sys.cpu.model` | CPU 型号/核数 | `lscpu`、`/proc/cpuinfo` |
| `sys.mem.model` | 内存型号/条数 | `dmidecode -t memory`（需 sudo）+ `free -h` |
| `sys.hw.overview` | 整机硬件概况 | `lshw -short`、`dmidecode -t system` |
| `sys.config.summary` | 系统配置摘要 | OS + CPU + 内存 + 磁盘一览 |
| `sys.bios.info` | BIOS/主板 | `dmidecode -t bios/baseboard` |
| `sys.block.devices` | 磁盘型号/拓扑 | `lsblk -o ...MODEL` |

规则总数现为 **64**。开发态 `/reload` 或重启即可；已打好的 tar 包需重打包或覆盖包内 `resources/rules.json`。
