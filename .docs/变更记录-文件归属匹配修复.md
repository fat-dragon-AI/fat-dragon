# 变更记录：文件归属匹配修复

日期：2026-08-13

## 问题

口语「文件归属」无法命中命令：仅有短词「归属」时分数低于弱匹配阈值；且缺少「只查看属主」意图。

## 变更

1. 新增 `perm.owner.view`：查看属主/权限（`ls -lah` → `stat` → `namei`）
2. 关键词覆盖：`文件归属`、`看文件归属`、`文件是谁的` 等
3. `perm.chown` 补充：`改文件归属` / `修改文件归属` 等修改类说法
4. 语料、README、核心 Top-1 回归补充

## SQL

无。
