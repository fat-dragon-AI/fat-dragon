# 变更记录：评审 P0/P1 落地

| 项 | 内容 |
|----|------|
| 日期 | 2026-08-11 |
| 范围 | shell 逃逸、手输风险、会话 cwd、extract、模板、单测、设计口径 |

## P0

1. **Shell 逃逸收紧**  
   - 查询模式：`!`/`！` **一律确认**  
   - Agent：普通免确认；`rm -rf` / `mkfs` / `dd of=/dev` 等高危仍确认  
   - 审计：`explicit_bang`；免确认时 `confirmed=false`  

2. **agent> 手输风险**  
   - 不再继承当前意图 `risk_level`  
   - 使用 `estimate_manual_risk()`（low/medium/high）  

3. **自动化测试**  
   - `tests/test_shell_escape.py` / `test_extract.py` / `test_corpus.py`  
   - 运行：`PYTHONPATH=. python3 -m unittest discover -s tests -v`

## P1

1. **extract**：实现 `mode`；端口裸数字收紧；按 `rule_params` 白名单裁剪  
2. **会话 cwd**：`lch/session.py`；`!cd` 生效；`run_shell`/`step`/`script` 使用会话目录  
3. **Java 模板外置**：`resources/templates/java/{start,stop}.sh.tpl`、`app.service.tpl`  
4. **设计文档**安全口径与实现对齐  

## 验证

```bash
PYTHONPATH=. python3 -m unittest discover -s tests -v
```
