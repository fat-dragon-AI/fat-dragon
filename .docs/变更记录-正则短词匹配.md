# 变更记录：正则短词匹配

日期：2026-08-25

## 问题

只输入「正则」「写个正则」等短词时无命中：`regex.cheat` 原先只有「正则语法」等长关键词。

## 变更

- `resources/rules.d/37-regex.json`：为 `regex.cheat` 增加 `正则` / `正则表达式` / `写个正则` / `regex` 等，并提高短词权重
- `resources/dict/synonyms.json`：同义词组对齐
- 语料与 matcher 回归

## 说明

要「5个汉字」请直接说「5个汉字」或「汉字正则」；只说「正则」会进语法速查入口。
